---
description: REST JSON API, DTOs, error envelope, query filters, and pagination
globs: backend/**/api/**/*.java,frontend/lib/api/**/*.{ts,tsx}
alwaysApply: false
---

# API standards

Align with `spec/api-contract.md`.

REST API, JSON. Meaningful HTTP status codes. Consistent error responses. Bean Validation. No entity exposure.

## URL and methods

- Prefix: `/api`.
- Nouns, plural: `/api/tickets`, `/api/tickets/{id}/comments`, `/api/users`.
- `GET` read, `POST` create, `PATCH` partial update. No `PUT` for tickets in MVP. No `DELETE`.
- Path `{id}` is the ticket UUID, not `TCK-n`.

## JSON

- camelCase fields matching the contract (`reporterId`, `createdAt`).
- Enums as strings: `OPEN`, `MEDIUM`, `AGENT`.
- Timestamps ISO-8601 UTC (`...Z`).
- Do not expose JPA entities in responses; use DTOs.

## Query parameters

- Search and filter through query parameters (see list filters in the API contract).

## Pagination

- Pagination only if required by the spec. Ticket list paginates; do not paginate other endpoints unless the spec says so.
- Query: `page` (0-based, default 0), `size` (default 20, max 100).
- Body: `content`, `page`, `size`, `totalElements`, `totalPages`.

## Errors

Single envelope:

```json
{
  "code": "ILLEGAL_TRANSITION",
  "message": "Cannot move CLOSED to RESOLVED",
  "details": {}
}
```

| HTTP | `code` examples |
| --- | --- |
| 400 | `VALIDATION_ERROR` |
| 404 | `NOT_FOUND` |
| 409 | `ILLEGAL_TRANSITION`, `TICKET_CLOSED`, `OPTIMISTIC_LOCK` |

No stack traces in responses.

## Compatibility

- Do not remove or rename contract fields without updating `spec/api-contract.md` first.
- Additive optional fields are allowed if documented.
