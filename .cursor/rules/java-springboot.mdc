---
description: Java 21 / Spring Boot layering, DTOs, validation, and state-machine rules
globs: backend/**/*.java
alwaysApply: false
---

# Java / Spring Boot

Apply when writing or reviewing anything under `backend/`.

## Stack

- Java 21
- Spring Boot 3.x
- Spring Web, Validation, Data JPA
- PostgreSQL; Flyway for schema
- Constructor injection only; no field `@Autowired`

## Architecture

Controller → Service → Repository

```
com.jiralite.tickets
  api          # controllers, request/response DTOs
  domain       # entities, enums (TicketStatus, TicketPriority, UserRole)
  service      # use cases and state machine
  persistence  # Spring Data repositories
  error        # problem details / exception handlers
```

## Rules

- Controllers remain thin. They map HTTP and Bean Validation only. No JPA calls, no status-transition logic.
- Business logic belongs in services/domain components.
- Use DTOs for API requests/responses. Do not expose JPA entities directly.
- Use Bean Validation.
- Centralize exception handling.
- Use meaningful HTTP status codes.
- State transitions must be validated by backend business logic. Do not bypass the state machine.
- Prefer small, focused classes and methods.
- Follow standard Java naming conventions.
- Services own transactions (`@Transactional`), ticket keys, and `spec/state-machine.md`.
- Repositories stay as Spring Data interfaces unless a custom query is required.
- Map entities to DTOs defined in `spec/api-contract.md`.

## Persistence

- UUID primary keys; `OffsetDateTime` for timestamps in UTC.
- Optimistic locking on `Ticket.version`.
- Never use native SQL concatenated with user input.

## Errors

- `400` validation, `404` missing entity, `409` illegal transition or closed-ticket mutation or lock failure.
- Match the error envelope in `rules/api-standards.md`.

## Forbidden

- `eval`, runtime script engines, hardcoded passwords, `spring.jpa.hibernate.ddl-auto=update` in anything beyond local experiments (prefer Flyway).
- Business rules in controllers or `@Entity` listeners for status changes.
